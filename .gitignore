# Maven (keep JAR for restricted environments)
backend/.mvn/
backend/mvnw
backend/mvnw.cmd
backend/target/*.class
backend/target/maven-status/
backend/target/classes/
backend/target/*.jar.original
# Keep: backend/target/rag-api-1.0.0.jar (needed for restricted environments)

# Node (keep build and node_modules for restricted environments)
frontend/.pnp/
frontend/.pnp.js
frontend/node_modules/.cache/
# Keep: frontend/build/ and frontend/node_modules/ (needed for restricted environments)

# Logs
backend/logs/
backend/*.log
*.log

# IDE
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
config/config.env
*.env
.env.local

# Temporary files
*.tmp
*.bak
*.orig

# JAR files (keep only source, not built artifacts)
# Keep: backend/target/rag-api-1.0.0.jar (needed for restricted environments)
# backend/target/*.jar

# Python (keep wheel files for restricted environments)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# Keep: python-packages/*.whl (needed for restricted environments)
# Note: venv/ is excluded but can be created locally

